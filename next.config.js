/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuração para Netlify - usando standalone é compatível com o plugin Next.js do Netlify
  output: 'standalone',
}

module.exports = nextConfig
